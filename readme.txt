First run "yarn run build" or something like that
Then run "yarn run dev"

Publish
npm install -- to install all the packages needed
yarn build
npm login -- then enter your login and password
npm publish
